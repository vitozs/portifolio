/*let quadro_text = document.getElementById('texto')
let title = document.getElementById('title')
let quadro = document.querySelector('#quadro')
let box =  document.getElementsByClassName('lang-box')


function getLang(lang){
    if(!quadro.classList.contains('active')){
        quadro.classList.add('active')
    }
    
    for(let i=0; box.length > i; i++){
        box[i].addEventListener('mouseleave', ()=>{ 
            quadro.classList.remove('active')

    })
    }


    switch (lang){
        
        case "html":
            title.innerText = "HTML"
            quadro_text.innerText = 'HTML é uma linguagem de marcação utilizada na construção de páginas na Web.'
            console.log(lang)
        break;
        case "css":
            title.innerText = "CSS"

            quadro_text.innerText = 'CSS é usado para estilizar elementos escritos em uma linguagem de marcação como HTML.'
            console.log(lang)
        break;
        case "bootstrap":
            title.innerText = "Bootstrap"

            quadro_text.innerText = 'Bootstrap é um framework web com código-fonte aberto para desenvolvimento de componentes de interface e front-end para sites e aplicações web, usando HTML, CSS e JavaScript, baseado em modelos de design para a tipografia, melhorando a experiência do usuário em um site amigável e responsivo.'
            console.log(lang)
        break;
        case "js":
            title.innerText = "Javascript"

            quadro_text.innerText = 'JavaScript é uma linguagem de programação interpretada estruturada, de script em alto nível com tipagem dinâmica fraca e multiparadigma. Juntamente com HTML e CSS, o JavaScript é uma das três principais tecnologias da World Wide Web.'
            console.log(lang)
        break;
        case "php":
            title.innerText = "PHP"

            quadro_text.innerText = ' PHP é uma linguagem de programação utilizada por programadores e desenvolvedores para construir sites dinâmicos, extensões de integração de aplicações e agilizar no desenvolvimento de um sistema.'
            console.log(lang)
        break;
        case "sql":
            title.innerText = "SQL"

            quadro_text.innerText = 'Structured Query Language, ou Linguagem de Consulta Estruturada ou SQL, é a linguagem de pesquisa declarativa padrão para banco de dados relacional. Muitas das características originais do SQL foram inspiradas na álgebra relacional.'
            console.log(lang)
        break;
        case "git":
            title.innerText = "GIT"

            quadro_text.innerText = ' GIT é um sistema de controle de versões distribuído, usado principalmente no desenvolvimento de software, mas pode ser usado para registrar o histórico de edições de qualquer tipo de arquivo.'
            console.log(lang)
        break;
    

    }
    
}
*/
